namespace Algorithms.Shared
{
    public static class BenchmarkHelper
    {
        public static void StartBenchmark(string benchmarkName)
        {
            Console.WriteLine();
            Console.WriteLine($"-== Starting {benchmarkName} Benchmark ==-");
        }
    }
}
